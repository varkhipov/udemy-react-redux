// History package was installed automatically with react-router-dom.
// This custom instance works exactly the same way as default, nothing is overridden.
// The only purpose of this is to be able to get its instance everywhere
// to be able to toggle navigation manually.
import { createBrowserHistory } from 'history';
export default createBrowserHistory();
